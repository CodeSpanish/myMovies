{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Country list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Isocode2</th>
                <th>Isocode3</th>
                <th>Shortname</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('country_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.isoCode2 }}</td>
                <td>{{ entity.isoCode3 }}</td>
                <td>{{ entity.shortName }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('country_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('country_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('country_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
